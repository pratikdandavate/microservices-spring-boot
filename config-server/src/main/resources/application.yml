server:
  port: 8888

spring:
    cloud:
        config:
            server:
                git:
                    uri: git://aiicsv02/microservices-poc.git
                    searchPaths: config-properties